export default async function sitemap() {
  const dataX = [
    {
      url: "https://seyantek.com",
      lastModified: new Date(),
      changeFrequency: "yearly",
      priority: 1,
    },
  ];

  return dataX;
}
